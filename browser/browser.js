// Browser entry generated from shared ComboCSS source.
// Keep this file thin so browser and Node builds use the same implementation.
import standalone from "../lib/standalone.js";

export { standalone as combocss };
export default standalone;
